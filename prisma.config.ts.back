// prisma.config.ts
import 'dotenv/config'
import { defineConfig, env } from 'prisma/config'

export default defineConfig({
  schema: 'libs/api/core/prisma/schema.prisma',
  migrations: {
    path: 'libs/api/core/prisma/migrations',
  },
  datasource: {
    url: env('DATABASE_URL'),
  },
})
